source 'https://rubygems.org'

gem 'sinatra', require: 'sinatra/base'

group :development do # these are for things to be done on local server
  gem 'shotgun' # doesn't require you to keep restarting server
  gem 'tux' # is like debugger console
end